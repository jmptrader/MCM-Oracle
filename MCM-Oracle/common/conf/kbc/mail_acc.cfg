<MAIL>

  DEFAULT_SENDER = tsmurex@kbc.be

  <GROUPS>

    ################################################
    # 4) user query account checking 
    ################################################
    <account_checking>
      address = tsmurex@kbc.be
    </account_checking>

    ################################################
    # 10) SQL query voor MA check
    ################################################
    <ma_check>
      address = tsmurex@kbc.be
    </ma_check>

    ################################################
    # 12) gbs_corporate@kbc.be
    ################################################
    <gbs_corporate>
      address = tsmurex@kbc.be
    </gbs_corporate>

    ################################################
    # 14) vcr
    ################################################
    <vcr>
      address = tsmurex@kbc.be
    </vcr>

    ################################################
    # 18) Monitoring bo expired with no fixings
    ################################################
    <bo_exp_fix_euro>
      address = tsmurex@kbc.be
    </bo_exp_fix_euro>

    <bo_exp_fix_asia>
      address = tsmurex@kbc.be
    </bo_exp_fix_asia>

    ################################################
    # Absolut Bank
    ################################################
    <absolut>
      address = tsmurex@kbc.be
      address = absmurex@kbc.be
    </absolut>

    ################################################
    # Aff feedback
    ################################################
    <aff_feedback>
      address = tsmurex@kbc.be
      address = confofxmm@kbc.be
    </aff_feedback>

    ################################################
    #  Monitoring for marketdata files import/export
    ################################################
    <export_marketdata>
      address = tsmurex@kbc.be
    </export_marketdata>

    <import_marketdata>
      address = tsmurex@kbc.be
    </import_marketdata>

    ################################################
    # Wrb macros
    ################################################
    <wrb_macros>
      address = tsmurex@kbc.be
    </wrb_macros>

    ################################################
    # FDE mkd mapping issues
    ################################################
    <fde_mapped>
      address = tsmurex@kbc.be
    </fde_mapped>

  </GROUPS>

</MAIL>
