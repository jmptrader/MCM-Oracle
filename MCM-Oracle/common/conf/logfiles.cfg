<LOGFILES>

  <SERVICES>
    filename          = logs/fxc*.log

    fail_pattern      = "ERROR"
    fail_addresses    = mario.truyens@lchclearnet.com
    fail_action       = 

    warn_pattern      = "WARN"
    warn_addresses    = mario.truyens@lchclearnet.com
    warn_action       =

    timeout           =
    timeout_pattern   =
    timeout_addresses =
    timeout_action    =
    timeout_flag      =
  </SERVICES>

</LOGFILES>
